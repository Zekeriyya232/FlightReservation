@model List<RoutesVM>
@{

}

<div class="card">
    <div class="card-header">
        <strong>Uçuş Ara</strong>
    </div>
    <div class="card-body">
        <div>
            <input list="guzergah">
            <datalist id="guzergah">
                @foreach(RoutesVM routes in Model)
                {
                    <option value="@routes.havalimaniKalkis"/>
                }  

            </datalist>
            @for(int i = 0; i < Model.Count; i++)
            {
                
            }
        </div>
       

    </div>
</div>